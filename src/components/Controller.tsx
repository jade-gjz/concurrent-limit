import { Button, Space } from 'antd'

const Controller = () => {
  return (
    <Space>
      <Button type="primary">上传一个文件</Button>
      <Button type="primary">上传两个文件</Button>
      <Button type="primary">上传三个文件</Button>
    </Space>
  )
}

export default Controller
